{
  "name": "shared-entities-module",
  "version": "0.0.4",
  "description": "Shared module containing types and utilities the project",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "keywords": [
    "shared",
    "module",
    "my-project"
  ],
  "scripts": {
    "build": "rm -rf dist && npx tsc"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.2",
    "nodemon": "^2.0.22"
  }
}
